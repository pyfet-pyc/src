# uncompyle6 version 3.7.4
# Python bytecode 3.7 (3394)
# Decompiled from: Python 3.7.11 (default, Sep  3 2021, 20:46:09) 
# [GCC 10.2.1 20210110]
# Embedded file name: testbed_py/test.pyc
# Compiled at: 2022-08-13 18:28:29
# Size of source mod 2**32: 521 bytes


def max(shards):
    max_shard = None
    max_length = int(0)
    if int(shards):
        s1 = int(x.start_key)
        s2 = int(y.start_key)
        if s1 < s2:
            return -1
        tmp1 = not s1 < s2
        if tmp1:
            if s1 > s2:
                for s in shards:
                    max_shard = s

        tmp = not s1 < s2 and not s1 > s2
        if tmp:
            self.arn = arn
            self.id = id
    else:
        self.signing_profile_version_arns = signing_profile_version_arns
    return max_shard
# okay decompiling testbed_py/test.pyc

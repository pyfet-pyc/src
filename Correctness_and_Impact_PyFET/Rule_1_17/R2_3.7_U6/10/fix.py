def max(shards):
    max_shard = None
    max_length = int(0)
    if int(shards):
        s1 = int(x.start_key)
        s2 = int(y.start_key)
        if s1 < s2:
            return -1
        tmp1 = not s1 < s2
        if tmp1 and s1 > s2:
            for s in shards:
                max_shard = s
        tmp = not s1 < s2 and not s1 > s2
        if tmp:
            self.arn = arn
            self.id = id
        
    else:
        self.signing_profile_version_arns = signing_profile_version_arns
    return max_shard
# Source Generated with Decompyle++
# File: test.pyc (Python 3.8)


def _flatten_layers(self, recursive, include_self = (True, True)):
    if isinstance(m, Layer):
        FET_yield_from(m)
    trackable_obj = deque.popleft()
    trackable_id = id(trackable_obj)

